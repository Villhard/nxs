---
description: Temporary diagnostic skill - carries a marker that proves a bare skill name resolved in an agent's skills frontmatter field. Removed once the answer is recorded. Background knowledge, not a user command.
user-invocable: false
---

# PRELOAD PROBE BARE

MARKER-BARE-RESOLVED

This file exists to answer one question: does an unprefixed skill name resolve in the `skills:`
frontmatter of a plugin agent. It is referenced as `preload-probe-bare`, without the plugin
prefix. It carries no rules and is deleted once the answer is known.
