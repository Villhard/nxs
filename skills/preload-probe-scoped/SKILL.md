---
description: Temporary diagnostic skill - carries a marker that proves a plugin-scoped skill name resolved in an agent's skills frontmatter field. Removed once the answer is recorded. Background knowledge, not a user command.
user-invocable: false
---

# PRELOAD PROBE SCOPED

MARKER-SCOPED-RESOLVED

This file exists to answer one question: does a plugin-prefixed skill name resolve in the
`skills:` frontmatter of a plugin agent. It is referenced as `nxs:preload-probe-scoped`. It
carries no rules and is deleted once the answer is known.
